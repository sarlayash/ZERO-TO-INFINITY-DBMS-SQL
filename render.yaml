services:
  - type: web
    name: zero-to-infinity-dbms-sql
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true
